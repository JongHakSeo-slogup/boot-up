import styled from "styled-components";

const styles = {
    Wrapper: styled.div`
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100vw;
      min-height: 100vh;
      background: #eee;
   `,
    Layout: styled.div`
      position: relative;
      width: 360px;
      height: 640px;
      background: #fff;
    `
}

export default styles;